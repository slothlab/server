type Query {
  users: [User!]!
  user(id: ID!): User!
}

type User {
  id: ID! @unique
  name: String!
  email: String! @unique
}

type Todo {
  id: ID! @unique
  title: String!
  completed: Boolean
}
